If this folder is empty, you've not run `npm install` yet. If you want to use
vagrant, you can leave your local node_modules empty; vagrant will mount a
directory in the guest system to shadow its contents.
